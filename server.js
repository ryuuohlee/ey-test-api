require('dotenv')
const express = require('express');


//route configs
const app = express();
const port = 3000;

//controllers


//routes

app.listen(port, () => {
  console.log(`Port is listening on port ${port}`);
})